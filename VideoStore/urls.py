from django.urls import path
# from .views import VideoProjectListCreate, VideoProjectRetrieveUpdateDestroy

urlpatterns = [
    # path('video-projects/', VideoProjectListCreate.as_view(), name='video-project-list-create'),
    # path('video-projects/<int:pk>/', VideoProjectRetrieveUpdateDestroy.as_view(), name='video-project-detail'),
]
